defmodule BeamDcd do
  @moduledoc """
  BEAM Dead Code Detector — a static analysis tool that detects unused public
  functions across BEAM language projects (Elixir, Erlang, Gleam, LFE).

  Operates directly on compiled `.beam` files rather than source code,
  making it language-agnostic across the entire BEAM ecosystem.
  """
end
